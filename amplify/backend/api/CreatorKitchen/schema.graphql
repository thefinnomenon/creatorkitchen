# Cognito User @owner can CRUD their own posts, Anyone can read any post
type Post @model @auth(rules: [{ allow: owner}, { allow: public, operations: [read] }]) {
  id: ID!
  content: String!
  media: AWSJSON
}
